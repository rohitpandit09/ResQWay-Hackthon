services:
  redis:
    image: redis:latest
    
    ports:
      - "6379:6379"

  backend:
    build:
      context: ./Backend
    
    ports:
      - "5000:5000"
    env_file:
      - ./Backend/.env
    depends_on:
      - redis